---
title: "How to create custom VS Code snippets"
type: "snippet"
description: "Speed up your workflow with custom code patterns in Visual Studio Code"
date: 2024-07-30
tags: ["vscode", "config"]
category: Tools
index: []
published: true
---

{/* <h2 className="text-[22px] !mt-0">What is a snippet ?</h2> */}

A snippet in VS code is a template of code that you can invoke with a defined keyword.
VS Code provides snippets through built-in features and extensions. You can also create 
custom snippets to fit your needs :

- Open your Command Palette : <KeyboardKey value="CMD" /> / <KeyboardKey value="CTRL" />  + <KeyboardKey value="SHIFT" /> + <KeyboardKey value="P" />
- Search for "Snippets: Configure User Snippets"
- Select the file type that you need (Global / for a project / for a language)

## Structure

```json {2}#purple {3}#green {4}#blue {8}#red
"Print to console": {
  "scope": "javascript,typescript",
  "prefix": "log",
  "body": [
    "console.log('$1');",
    "$2"
  ],
  "description": "Log output to console"
}
```
<div className="article-layout">
  <ColorLegend color={"purple"}>
    The `scope` defines where this snippet is applicable
  </ColorLegend>

  <ColorLegend color={"green"}>
    The `prefix` is the acronym that you will type to invoke the snippet
  </ColorLegend>

  <ColorLegend color={"blue"}>
    The `body` contains the code that will be displayed
  </ColorLegend>

  <ColorLegend color={"red"}>
    The `description` will show up in the autocomplete suggestions
  </ColorLegend>
</div>

## Tab stops

- `$1`, `$2` : cursor positions (press <KeyboardKey value='TAB' /> to switch)
- `$0` : final cursor stop

## Default values for tab stops
- `${1:default value}` : Pre-fills with a value, ready to overwrite

## Multiple options for default values
- `${2|log,error,table|}` : Choose between `log`, `error`, `table`

<SpacerS />